{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"nodemon src/server.js\" \"cd ../build && pnpm start\"",
    "dev":  "concurrently \"nodemon src/server.js\" \"cd ../build d&& pnpm start\"" ,
    "build": "node src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "pnpm install concurrently"},
  "dependencies": {
    "bcrypt": "^5.1.1",
    "body-parser": "^1.20.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.11.0",
    "mongoose": "^8.8.2",
    "nodemon": "^3.1.7",
    "signale": "^1.4.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "type": "module"
}
